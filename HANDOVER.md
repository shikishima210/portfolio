# 引き継ぎメモ — 杉島輝 ポートフォリオサイト

## これは何か
名古屋文理大学 情報メディア学科3年 杉島輝（28卒）の就活用ポートフォリオ。
グラフィックデザイン × プロダクトデザイン。紙器・パッケージ業界が第一志望。

## ファイル
```
index.html     ページ全体（HTML / CSS / JS が1ファイルに入っている）
images/        作品画像 21点
```

## 最初にやるといいこと
```bash
git init && git add -A && git commit -m "初期状態"
```
以降は必ず動く状態をコミットしてから触る。

## 構造（ここが一番大事）
縦スクロールのページではなく、**FigJamのような盤面**。
`#viewport`（固定）の中の `#canvas` を transform で移動・拡大する。

```
   ABOUT  ←    TOP    →  WORKS
                ↓
             SKILLS
                ↓
             footer
```
- 板の座標は**JSが実測して決める**。CSSに座標を書かない（書くと中身の高さが変わったとき重なる。実際に一度やらかした）
- 該当箇所：`/* 板を実測して並べる（TOPが中心） */` の `layout()`
- 板をつなぐ破線と矢印も同じ関数が引き直す（`drawLinks()`）

## 主要なスクリプト（index.html 内、上から順）
| 印 | 役割 |
|---|---|
| `const IMG = {` | 画像のパス表。ここに追加しないと作品が出ない |
| `const W = {` | 作品データ。課題・やったこと・結果・制作期間・ツール |
| `const FLOATING = [` | TOPに浮かぶ8点の配置。黄金長方形の対角線上に置いている |
| `/* 平面の壁の上に… */` | three.js。浮かぶ作品の描画と当たり判定 |
| `/* やわらかい光 */` | カーソルに遅れてついてくる光 |
| `/* 付箋（つまんで動かす・はがす） */` | TOPの3枚の付箋 |
| `/* 盤面をつかんで動かす */` | pan / zoom / focus。`window.__focus(name)` で外から呼べる |
| `/* 板を実測して並べる */` | レイアウト計算 |
| `/* 操作スティック */` | 右下のスティック。`window.__pan(dx,dy)` を叩いている |

## 作品を1点足すとき
1. `images/` に画像を置く
2. `IMG` にキーを追加
3. `W` に作品オブジェクトを追加（`one` は一覧に出る一行）
4. TOPに浮かせたいなら `FLOATING` にも追加
5. `ORDER.product` と `ORDER.graphic` の配列にIDを入れる（両方）

## 未完了
- [ ] `your@email.com` を実際のアドレスに
- [ ] Instagram / X のリンク（今は `#`）
- [ ] 作品の「課題 / やったこと / 結果」の事実確認。特に**QRコード看板は3年生がデザイン、杉島が留め具と展示方法**という役割分担を面接で説明できるように
- [ ] スマホ表示の確認（860px以下は盤面をやめて縦積みに切り替わる）
- [ ] 画像の重さ（合計3.5MB）。表示が重ければ WebP 化

## 触るときの注意
- 盤面の座標を**CSSに固定値で書かない**
- 紙の質感（シミ・擦れ・折り目）は**背景 `.bg` だけ**に。前面のパネルに重ねると汚れに見える
- カーソルに追従して**背景そのものを歪ませない**（一度やって「気持ち悪い」と却下されている）。光が遅れてついてくる程度に留める
- three.js は r128。`CapsuleGeometry` など新しいものは使えない

## 設計の前提（なぜこうなっているか）
- 人事は1冊5分前後、冒頭数ページで判断する。だから**採用実績5点を最初に数字で出す**
- WORKSは受ける会社で順番を入れ替えられる（`ORDER`）。紙器なら立体から、制作会社ならグラフィックから
- 見えている文字は最小限。詳細はクリックした先に置く
